// Formatting lives in core so the dashboard and the SVG card always print the same thing.
export { WEEKDAYS, MONTHS_SHORT, MONTHS_LONG, fmtInt, fmtHours, fmtDuration, fmtDate, fmtDay, fmtMonth } from "@trackhour/core";
